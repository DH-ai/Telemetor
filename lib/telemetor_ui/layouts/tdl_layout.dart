/// Fixed layout dimensions for mission-control shells.
///
/// Widths are structural (not spacing-scale) but heights align to [TDLSpacing].
class TDLLayout {
  const TDLLayout._();

  static const double sidebarWidth = 220;
  static const double rightRailWidth = 280;
  static const double headerHeight = 48;
  static const double statusStripHeight = 28;
}
